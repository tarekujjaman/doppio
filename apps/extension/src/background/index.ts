// TODO(M8): action click → chrome.tabCapture.getMediaStreamId → offscreen document
// records via MediaRecorder (webm/opus) → on stop, upload through the portal's
// signed-URL/ingest API (source=EXTENSION, title = tab title).
export {};
