# Build-time config for the extension (baked into the bundle).
# The anon key is a publishable key (already shipped in the web client), safe to embed.
VITE_APP_URL="https://doppio-gamma.vercel.app"
VITE_SUPABASE_URL="https://<project-ref>.supabase.co"
VITE_SUPABASE_ANON_KEY=""
