# Keep the Gradle wrapper script LF so it runs on Linux CI; Windows script CRLF.
gradlew        text eol=lf
*.sh           text eol=lf
gradlew.bat    text eol=crlf
*.bat          text eol=crlf

# Binary
*.jar          binary
*.keystore     binary
*.png          binary
*.ttf          binary
*.otf          binary
