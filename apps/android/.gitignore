# Android / Gradle
*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
*.apk
*.aab
*.keystore
*.jks
keystore.properties
!debug.keystore

# Hilt/KSP/Room generated
/app/build/

# Firebase (release only; do not commit real config)
/app/google-services.json

# Keep the Gradle wrapper jar (intentionally committed)
!gradle/wrapper/gradle-wrapper.jar

# Release signing — never commit the key or its password
*.jks
keystore.properties
